name: Publish daily fortune

on:
  workflow_dispatch:

jobs:
  fortune:
    runs-on: ubuntu-latest
    steps:
      - name: Pull image
        run: |
          docker pull ghcr.io/wivik/fortune:latest

      - name: Generate fortune
        run: |
          docker run --rm -v ${{ github.workspace }}:/fortune/output ghcr.io/wivik/fortune:latest -w https://fortune.zedas.fr

      - run: find .
        

